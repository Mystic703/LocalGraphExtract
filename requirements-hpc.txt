# requirements-hpc.txt — CEA CCRT A100 (vLLM backend, offline)
# Install on a login node (with internet): pip install -r requirements-hpc.txt

# Core LlamaIndex
llama-index>=0.10.0
llama-index-core>=0.10.0

# vLLM backend (A100 / H100)
# vLLM must match the CUDA version loaded via `module load cuda/X.Y`
# Check latest wheel at: https://docs.vllm.ai/en/latest/getting_started/installation.html
vllm>=0.4.0
llama-index-llms-vllm>=0.1.0

# HuggingFace fallback
llama-index-llms-huggingface>=0.1.0
transformers>=4.40.0
accelerate>=0.28.0
safetensors>=0.4.0

# Embeddings
llama-index-embeddings-huggingface>=0.1.0
sentence-transformers>=2.7.0

# Document loading (run on login node or pre-process locally)
docling>=1.0.0
pypdf>=4.0.0

# HuggingFace Hub (download only — offline at job runtime)
huggingface-hub>=0.22.0

# Utilities
nest-asyncio>=1.6.0
